<%= form_with(model: user_note, local: true) do |form| %>
  <% if user_note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_note.errors.count, "error") %> prohibited this user_note from being saved:</h2>

      <ul>
      <% user_note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id_user %>
    <%= form.number_field :id_user, id: :user_note_id_user %>
  </div>

  <div class="field">
    <%= form.label :id_note %>
    <%= form.number_field :id_note, id: :user_note_id_note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
